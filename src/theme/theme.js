import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#FF5C00',
    },
  },
  background: {
    dark: '#222222',
    light: '#FFF8F5',
  },
});

export default theme;
